// Cấu hình thông tin đăng nhập
const CONFIG = {
    EMAIL: 'nvtn@gmail.com',
    PASSWORD: 'nvtn12345'
};

// Export để sử dụng trong các file khác
if (typeof module !== 'undefined' && module.exports) {
    module.exports = CONFIG;
} 